zig-out/
zig-out-*/
.zig-cache/
*.png
!docs/*.png
.DS_Store
# Xcode / xcodegen build products and derived data (build, build-ios,
# build-iosdev, DerivedData, …) — never tracked.
macos/build*/
macos/DerivedData/
# iOS builds of the native deps (obsolete since the Metal port; kept ignored
# in case an old checkout still has them).
ios-deps/
