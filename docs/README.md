# lookout documentation

Source for the lookout-core documentation site, built with
[Docusaurus](https://docusaurus.io/).

## Develop

```sh
npm install
npm start
```

## Build

```sh
npm run build
npm run serve   # preview the production build
```

Content pages live under `docs/`; the sidebar order is in `sidebars.js`.
