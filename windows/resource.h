#pragma once
// Win32 resource ids for LookoutMarine.rc.
//
// IDI_APPICON is 1 deliberately: Explorer, the taskbar and the Alt-Tab list
// show the ICON resource with the LOWEST id, so the app icon has to hold it.
#define IDI_APPICON 1
