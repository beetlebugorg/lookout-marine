# Build outputs + local config (regenerated by build-libs.sh / gradlew).
/local.properties
/.gradle/
/build/
/app/build/
/app/.cxx/
# Prebuilt Zig core archives — rebuilt by ./build-libs.sh per ABI.
/app/jni/prebuilt/
